# Environment Policy

Every deployable project must define environment boundaries before production.

## Baseline

- Local and dev may use disposable data.
- Staging should be production-like.
- Production requires approval, observability, rollback and incident contacts.
